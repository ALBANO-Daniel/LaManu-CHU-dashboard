<?php

// <!-- 1 ctrl 1 functinatlite 1 view  -->
// require_once config
// require_once patient model


// fisrt time it loads, tehre is a GET method, because tehre is a Form? or because is php archive... ?
// if($_Server['request_method'] == 'POST'){

    // netoyage et validation DATA

    // nom,prenom, gnere, n securite social, date naissance, adress, code post,
    // add a new if to test error 
    //if(empty($error)))
    try{
        $msqlconnection = new PDO(
            dsn,
            user,
            pwd,
        );

    }


'else', goes to show inclues views... 
// }


// include ... all views